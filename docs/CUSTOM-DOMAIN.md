# Custom Domain

Do not add a `CNAME` file until you own and configure a domain.

After purchasing a domain:

1. Configure the domain in GitHub repository **Settings → Pages**.
2. Follow GitHub's DNS instructions.
3. GitHub will create or require a `CNAME` file containing only your domain, for example:

```text
rajcloudconsulting.co.uk
```
